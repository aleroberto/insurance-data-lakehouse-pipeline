[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "insurance-lakehouse"
version = "0.1.0"
description = "Pipeline de dados utilizando arquitetura Lakehouse"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]